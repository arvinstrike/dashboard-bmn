import './bootstrap';

// Custom Alert System is loaded via HTML
// Available globally as: Swal.fire(), Swal.success(), Swal.error(), Swal.warning(), Swal.info()
// Or: CustomAlert.fire(), CustomAlert.toast()

